FROM golang:1.24-alpine

WORKDIR /app

# 安装 air
RUN go install github.com/air-verse/air@latest

# 复制依赖文件
COPY go.mod go.sum ./
RUN go mod download

# 复制源代码
COPY . .

# 暴露端口（根据你的 config.yaml 或代码中的实际端口修改）
EXPOSE 8080

CMD ["air", "-c", ".air.toml"]
